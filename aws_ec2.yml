plugin: aws_ec2
regions:
  - us-east-1
filters:
  tag:app: sap_hana_ha_demo
hostnames:
  - network-interface.association.public-ip
keyed_groups:
  - prefix: tag
    key: tags
compose:
  ansible_user: "'root'"