---
nodes:
  - name: s4hana
    role: s4app
    app_name: sap_hana_ha_demo
    pod_name: "{{ ec2_name_prefix }}"
    instance_type: m5.4xlarge
    image: "{{ ec2_ami_ids['rhel8'] | default('ami-02eac2c0129f6376b') }}" #rhel8
    network_source_dest_check: no
    networks:
      - ip: 10.10.10.101
    volumes:
      - device_name: /dev/sda1
        ebs:
          volume_size: 45
          delete_on_termination: true
      - device_name: /dev/xvdb
        ebs:
          volume_size: 400
          delete_on_termination: true

  - name: hana1
    role: hana
    app_name: sap_hana_ha_demo
    pod_name: "{{ ec2_name_prefix }}"
    instance_type: m5.4xlarge
    image: "{{ ec2_ami_ids['rhel8'] | default('ami-02eac2c0129f6376b') }}" #rhel8
    network_source_dest_check: no
    networks:
      - ip: 10.10.10.102
    volumes:
      - device_name: /dev/sda1
        ebs:
          volume_size: 45
          delete_on_termination: true
      - device_name: /dev/xvdb
        ebs:
          volume_size: 400
          delete_on_termination: true

  - name: hana2
    role: hana
    app_name: sap_hana_ha_demo
    pod_name: "{{ ec2_name_prefix }}"
    instance_type: m5.4xlarge
    image: "{{ ec2_ami_ids['rhel8'] | default('ami-02eac2c0129f6376b') }}" #rhel8
    network_source_dest_check: no
    networks:
      - ip: 10.10.10.103
      - ip: 10.10.10.100
    volumes:
      - device_name: /dev/sda1
        ebs:
          volume_size: 45
          delete_on_termination: true
      - device_name: /dev/xvdb
        ebs:
          volume_size: 400
          delete_on_termination: true